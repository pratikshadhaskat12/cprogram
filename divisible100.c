#include <stdio.h>
int main()
       int number;
       printf("Enter the number: "); 
       scanf("%d", &year);
      
       if (N % 100 == 0) {
       ptintf("%d not divisible by 100.\n", year);
        } else {
            printf("%d divisible by 100.\n", year);
               }
               return 0;
               }
