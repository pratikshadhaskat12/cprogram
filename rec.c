#include <stdio.h>
int main ()
{
          float base, height, area;
          scanf("%f", &base);
          scanf("%f", &height);
          area  =  (base * height) / 2;
          printf(" area of triangle = %.2f sq. units", area);
          return 0;
          }
