#include <stdio.h>
int main() 
{
         float calculateSimpleInterest(float amount,float rate,int time)
         float interest = (amount * rate         
         printf("Enter the amount: ");
         scanf("%f", &amount);
         
         printf("The enter rate of intrest; ");
         scanf("%f", &rate);
         
         printf("Enter the time (in years): ");
         scanf("%f, &time);
         
         intrest = (amount * rate * time) / 100;
         
         printf("The simple intrest is: %.2f\n, intrest);
         return 0;
         }

         
         
         
